import React from 'react';import { makeStyles, Theme, createStyles } from '@material-ui/core/styles';import Modal from '@material-ui/core/Modal';import Backdrop from '@material-ui/core/Backdrop';import Fade from '@material-ui/core/Fade';import {DelectButton,DelMod,BtnDel,Yes,No,Div,DivOne} from '../product'const useStyles = makeStyles((theme: Theme) =>    createStyles({        modal: {            display: 'flex',            alignItems: 'center',            justifyContent: 'center',        },        paper: {            backgroundColor: theme.palette.background.paper,            border: '2px solid #000',            boxShadow: theme.shadows[5],            padding: theme.spacing(2, 4, 3),        },    }),);function DeleteModal({del}) {    const classes = useStyles();    const [open, setOpen] = React.useState(false);    const handleOpen = () => {        setOpen(true);    };    const handleClose = () => {        setOpen(false);    };    function dell(){        del();        setOpen(false)    }    return (        <span style={{border:'none',background:'none'}}>            <DelectButton onClick={handleOpen}>                <i className="bi bi-trash mx-1"> </i>            </DelectButton>            <Modal                aria-labelledby="transition-modal-title"                aria-describedby="transition-modal-description"                className={classes.modal}                open={open}                onClose={handleClose}                closeAfterTransition                BackdropComponent={Backdrop}                BackdropProps={{                    timeout: 500,                }}            >                <Fade in={open}>                    <div>                        <DelMod>                             <No className={'float-end'} onClick={handleClose}>                                   <i className="fas fa-times"></i>                              </No>                                <DivOne><i className="fas fa-trash-alt"></i></DivOne>                            <BtnDel>                                  <Div>Ты действительно хочешь удалить?</Div>                                 <Yes onClick={dell}>                                  Удалить                              </Yes>                            </BtnDel>                        </DelMod>                    </div>                </Fade>            </Modal>        </span>    );}export default DeleteModal;